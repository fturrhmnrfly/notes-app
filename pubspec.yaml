name: notes_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Menambahkan dependensi untuk notifikasi lokal
  flutter_local_notifications: ^12.0.0
  sqflite: ^2.4.1
  path: ^1.8.3 # Menambahkan path sebagai tambahan dependensi baru

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
